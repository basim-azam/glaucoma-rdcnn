# @package _global_
# Reproduction config for DRISHTI-GS — table 1 row 1 of the paper.
defaults:
  - override /data: drishti_gs
  - override /model: rdcnn
  - override /training: single_gpu

run_name: repro_drishti_${now:%Y%m%d-%H%M%S}
trainer:
  max_epochs: 60
  precision: 16-mixed
